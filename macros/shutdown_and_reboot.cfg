[gcode_macro SHUTDOWN] # Macros para tirar abajo la maquina o rearrancar, usando rutinas de Klipper
gcode:
  {action_call_remote_method("shutdown_machine")}

[gcode_macro REBOOT]
gcode:
  {action_call_remote_method("reboot_machine")}